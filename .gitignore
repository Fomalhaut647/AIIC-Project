# pixi environments
.pixi/*
!.pixi/config.toml

# Sensitive: never commit certs / keys / archives that may contain them
*.zip
*.key
*.crt
*.pem
*.pfx
*.p12

# v2 runtime artifacts
data/sessions/*.json
logs/*.log
